spring.application.name=sena-empresa

#App Configuration
server.port=8080
server.servlet.context-path=/sena-empresa
spring.application.version=1.0.0

# JWT Configuration
jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION}
jwt.refresh-expiration=${JWT_REFRESH_EXPIRATION}

## CORS Configuration (Los métodos y cabeceras los manejaremos en SecurityConfig)
cors.allowed-origins=${CORS_ALLOWED_ORIGINS}
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=Authorization,Content-Type

#DB Configuration
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# Logging Configuration
logging.level.org.springframework.security.config.annotation.authentication.configuration.InitializeUserDetailsBeanManagerConfigurer=ERROR

#Langchain4j Configuration
langchain4j.open-ai.chat-model.api-key=demo
langchain4j.open-ai.chat-model.model-name=gpt-4o-mini

#OCI Configuration
oci.bucket-name=${OCI_BUCKET_NAME}
oci.namespace=${OCI_NAMESPACE}
oci.config-file=${OCI_CONFIG_FILE}


